# B RAG Freeze Plan

Goal: Freeze clean field_name + sample_values regulation RAG with rebuild ingestion.

- Import the existing rebuild extraction, quality, chunking and candidate ingestion modules.
- Add failing tests for directory leaders and decorative short lines while retaining headings and short blocks.
- Implement conservative cleanup; verify normative lines and titles are retained.
- Set all default classification entry points to clean Query; retain experimental presets for historical reproduction.
- Export a deterministic cleaned chunk snapshot and manifests from existing extracted text, preserving REVIEW provenance.
- Verify unit/API tests, lint, snapshot hashes and candidate collection. Document remaining OCR review explicitly.
- Commit and integrate/push main when repository permissions permit.
